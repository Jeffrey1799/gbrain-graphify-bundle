# 2026-08-28 工作日志

## gbrain-graphify-bundle：版本策略从固定版本改为跟随官方最新

用户要求将 skill 设计从「固定版本（pinned）」改为「以工具官方仓库最新版本为准」。

### 改动文件（15 个）
- `plugins/gbrain-graphify/scripts/versions.json`：移除 gbrain `0.42.67.0`+commit、graphify `0.9.30`，改为 `channel: "latest"`，保留 repository/package/extras/最低运行时要求
- `setup.sh` / `setup.ps1`：新增 `latest_gbrain_version()`（GitHub releases/latest → tags fallback，去 v 前缀）、`latest_graphify_version()`（PyPI JSON）；安装改用 `bun install -g github:garrytan/gbrain`（默认分支）和 `uv tool install graphifyy[extras]`（无 ==version）；已装版本落后于最新且无 `--upgrade` 时仍拒绝执行（fail-closed 保留）；离线/网络失败时 latest 为空 → 已装则放行
- `doctor.py`：新增 `_http_json`/`latest_gbrain_version`/`latest_graphify_version`（urllib 标准库，10s timeout）；版本检查 `versionCheck: ok|stale|unknown`，网络不可用降级 unknown；recovery 建议 `--upgrade` 仅当 stale
- 两个 SKILL.md + README×3 + plugins README×2 + plugin.json + THIRD_PARTY_NOTICES + TROUBLESHOOTING：pinned 表述全部改为 latest
- `tests/test_plugin.py`：`test_pinned_tool_manifest_is_consistent` → `test_tool_manifest_follows_latest`；版本冲突测试注入 `GBRAIN_GRAPHIFY_LATEST_GBRAIN=1.0.0` 保证离线确定性；新增 latest 函数单测（env override / 离线 fallback / v 前缀剥离 / PyPI 解析）

### 实测结果
- 真实网络查询：GBrain latest = 0.47.0.0，Graphify latest = 0.9.51（原固定版本已落后）
- 本机 doctor 实测：已装 gbrain 0.45.9.0 / graphify 0.9.40 正确标记 `stale`，recovery 建议 `bootstrap.ps1 -Agent codex -Upgrade`
- 单元测试 53 个：仅 `test_windows_bootstrap_dry_run` 失败（bootstrap.ps1 在本机 codex CLI 输出格式下的**既有问题**，该文件未改动，与本次无关）

### ⚠️ git 事故（重要）
验证过程中 `git stash push` 在沙箱中写入中断，导致 **v1.0.2 的 commit 对象（6f5381ef）丢失**，`.git/refs/` 目录消失。已恢复：
- main 指向 v1.0.1（2e92be78，pack 中仍存在）
- 工作区所有文件完好（含 v1.0.2 原始改动 + 本次 latest 改动），显示为未提交（18 个文件，部分 index 已 stage）
- **待办**：建议用户 `git add -A && git commit` 重新提交一个包含全部改动的 commit

### 关键经验
- 本仓库测试套件中 `test_windows_bootstrap_dry_run` 依赖本机 codex CLI，环境相关，跑全量测试前先确认
- 不要在沙箱环境对含 .git 的仓库做 git stash/gc 等写操作，写入中断会丢对象

## /update v1.0.3 提交（10:05）

用户执行 `/update`（作者 Jeffrey1799）完成版本更新并提交：
- 5 处版本号 1.0.2 → 1.0.3：`.claude-plugin/marketplace.json`、plugins 两个 `plugin.json`、`doctor.py` clientInfo、`tests/test_plugin.py` 断言
- 新建《版本记录.txt》记录 v1.0.3 变更
- 提交 `764e236` "v1.0.3: 版本策略改为跟随官方最新发布并升级插件版本"，作者 `Jeffrey1799 <19523330249@163.com>`，20 files，+323/-105，**未推送**
- 用户曾质疑"不是应该 1.0.2 吗"：澄清事实后确认——GitHub 上 origin/main 已是 v1.0.2（6f5381ef，之前推送过，内容不同），latest 策略作为新版本递增为 v1.0.3，用户确认**保持 v1.0.3**
- 补充：remote ref（refs/remotes/origin/main）因沙箱写入缺失，已手动重建指向 6f5381ef

## 推送远程（10:08）

用户要求推送。本地与远程在 v1.0.1 处分叉（remote 独有 6f5381e v1.0.2，本地独有 764e236 v1.0.3），普通 push 会被拒绝。
- 采用安全方案：`git merge origin/main` 合并远程 v1.0.2，5 个版本号冲突文件（marketplace.json、两个 plugin.json、doctor.py、test_plugin.py）统一取本地（1.0.3 + latest 内容），合并后测试通过
- 历史：`5cd5971`（merge）→ `d441071`（memory 日志）→ `764e236`（v1.0.3）→ `6f5381e`（v1.0.2）→ `2e92be7`（v1.0.1），本地领先远程 3 个提交，fast-forward 可推
- **推送失败**：GitHub 认证被拒（"Invalid username or token. Password authentication is not supported"），沙箱环境无交互凭据（credential.helper=helper-selector），PowerShell 通道输出异常
- **认证根因与解决**：gh CLI keyring 中的 token 失效（`gh auth status` 报 token invalid），SSH key 未注册 GitHub。用户按指引重新生成 PAT（scopes: repo + read:org + workflow）后 `gh auth login` 认证成功
- **推送成功（10:29）**：`git ls-remote origin main` = `5cd5971`，与本地一致，远程已同步 v1.0.3 + merge
- 经验：沙箱内 git push 无法完成 https 认证，认证类操作需用户本机执行；gh 认证要求 token 含 repo/read:org/workflow 三个 scope
